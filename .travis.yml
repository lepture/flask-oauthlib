language: python

python:
    - "2.6"
    - "2.7"
    - "3.3"
    - "3.4"
    - "pypy"

script:
    - py.test -s --cov flask_oauthlib --cov-report=term-missing

after_success:
    - pip install coveralls
    - coveralls

branches:
  only:
    - master

notifications:
    email: false
